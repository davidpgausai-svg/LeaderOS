Below are **clean, Campfire-style HTML pages** for **login**, **register**, and a simple **dashboard**.
They match Campfire’s design philosophy:

* Super lightweight
* No frameworks
* Minimal CSS
* Simple forms
* Works perfectly with your JWT + Node backend
* Looks professional, clean, and enterprise-simple

You can drop these directly into:

```
/src/pages/login.html
/src/pages/register.html
/src/pages/dashboard.html
```

---

# ⭐ **login.html (Campfire-style)**

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>StrategicFlow – Login</title>
  <style>
    body {
      font-family: sans-serif;
      max-width: 400px;
      margin: 60px auto;
    }
    h1 { font-size: 24px; margin-bottom: 20px; }
    input {
      width: 100%;
      padding: 10px;
      margin: 8px 0;
      font-size: 16px;
    }
    button {
      width: 100%;
      padding: 12px;
      font-size: 16px;
      background: black;
      color: white;
      border: none;
      cursor: pointer;
    }
    a { display: block; margin-top: 15px; text-align: center; }
  </style>
</head>
<body>
  <h1>Login to StrategicFlow</h1>

  <form id="loginForm">
    <input type="email" id="email" placeholder="Email" required />
    <input type="password" id="password" placeholder="Password" required />
    <button type="submit">Sign In</button>
  </form>

  <a href="/register">Create an account</a>

  <script>
    const form = document.getElementById('loginForm');

    form.addEventListener('submit', async (e) => {
      e.preventDefault();

      const payload = {
        email: document.getElementById('email').value,
        password: document.getElementById('password').value
      };

      const res = await fetch('/auth/login', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(payload)
      });

      const data = await res.json();

      if (data.token) {
        localStorage.setItem('jwt', data.token);
        window.location.href = '/dashboard';
      } else {
        alert(data.error || "Login failed.");
      }
    });
  </script>
</body>
</html>
```

---

# ⭐ **register.html (Campfire-style)**

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>StrategicFlow – Register</title>
  <style>
    body {
      font-family: sans-serif;
      max-width: 400px;
      margin: 60px auto;
    }
    h1 { font-size: 24px; margin-bottom: 20px; }
    input {
      width: 100%;
      padding: 10px;
      margin: 8px 0;
      font-size: 16px;
    }
    button {
      width: 100%;
      padding: 12px;
      font-size: 16px;
      background: black;
      color: white;
      border: none;
      cursor: pointer;
    }
    a { display: block; margin-top: 15px; text-align: center; }
  </style>
</head>
<body>
  <h1>Create your StrategicFlow account</h1>

  <form id="registerForm">
    <input type="email" id="email" placeholder="Email" required />
    <input type="password" id="password" placeholder="Password" required />
    <button type="submit">Create Account</button>
  </form>

  <a href="/login">Back to login</a>

  <script>
    const form = document.getElementById('registerForm');

    form.addEventListener('submit', async (e) => {
      e.preventDefault();

      const payload = {
        email: document.getElementById('email').value,
        password: document.getElementById('password').value
      };

      const res = await fetch('/auth/register', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(payload)
      });

      const data = await res.json();

      if (data.success) {
        alert("Account created! You may now log in.");
        window.location.href = '/login';
      } else {
        alert(data.error || "Registration failed.");
      }
    });
  </script>
</body>
</html>
```

---

# ⭐ **dashboard.html (Campfire-simple placeholder)**

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>StrategicFlow – Dashboard</title>
  <style>
    body {
      font-family: sans-serif;
      max-width: 600px;
      margin: 60px auto;
    }
    h1 { font-size: 28px; }
    button {
      padding: 10px 20px;
      background: black;
      color: white;
      border: none;
      cursor: pointer;
      margin-top: 20px;
    }
  </style>
</head>
<body>
  <h1>Welcome to StrategicFlow</h1>
  <p>This is your dashboard placeholder.  
     You are successfully logged in.</p>

  <button id="logoutBtn">Logout</button>

  <script>
    // redirect if no token
    if (!localStorage.getItem('jwt')) {
      window.location.href = '/login';
    }

    document.getElementById('logoutBtn').addEventListener('click', () => {
      localStorage.removeItem('jwt');
      window.location.href = '/login';
    });
  </script>
</body>
</html>
```

---

# ✔️ Why These Pages Are Correct

These pages are:

### ✔ 100% aligned with Campfire philosophy

Minimal HTML, minimal JS, fast to load, durable, long-lifecycle code.

### ✔ Perfect for DigitalOcean App Platform + Docker

No external frameworks = easier builds + no breakage.

### ✔ Works directly with your `/auth/register`, `/auth/login` APIs

You can drop them in straight away.

### ✔ Uses simple fetch() → JSON flow

Same as how modern self-hosted tools handle login.

### ✔ No need for templating systems

Agent will write everything exactly as-is.

---

# 👉 Next:

If you want, I can also generate:

### 🔹 Campfire-style CSS theme (optional, clean)

### 🔹 A better dashboard UI with Strategy → Projects → Actions navigation

### 🔹 A polished StrategicFlow homepage (optional)

### 🔹 Full “Account Settings” page

### 🔹 A “forgot password” workflow

Just tell me:
**“Give me the Campfire-style CSS theme.”**
